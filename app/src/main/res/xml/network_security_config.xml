<?xml version="1.0" encoding="utf-8"?>
<!--
    Network security configuration.

    Production backend (https://scamsheil-backend.onrender.com) uses HTTPS and
    does not require any special configuration here — Android allows HTTPS traffic
    to all domains by default.

    The cleartext rule below is kept only for local development with the emulator
    (10.0.2.2 is the host machine's localhost alias). It has no effect on
    production HTTPS traffic. Remove it if local-emulator testing is no longer needed.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
